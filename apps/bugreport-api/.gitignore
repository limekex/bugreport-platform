# ─────────────────────────────────────────────────────────
# bugreport-api — build artefacts and local state
# ─────────────────────────────────────────────────────────
dist/
*.tsbuildinfo
coverage/
logs/
tmp/
uploads/

# Local env overrides — keep .env.example tracked
.env
.env.local
.env.*.local

# Domain mapping data (runtime state, managed via admin UI)
data/
